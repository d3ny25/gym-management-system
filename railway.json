{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd gimnasio-app && composer install --no-interaction --prefer-dist && npm install --ignore-scripts && npm run build"
  },
  "deploy": {
    "startCommand": "cd gimnasio-app && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
